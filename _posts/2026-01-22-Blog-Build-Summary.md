---
title: 搭建 Chirpy 博客：GitHub Pages 部署与 Docker 本地开发
date: 2026-01-22 00:17:17 +0800
categories: [随手记, 博客搭建]
tags: [Chirpy, Jekyll, Docker, VS Code]
pin: false
math: false
mermaid: false
---
选择了 **[Chirpy](https://github.com/cotes2020/chirpy-starter)** 来搭建博客，本文将记录我参照 [Getting Started](https://chirpy.cotes.page/posts/getting-started/) 官方教程进行配置的全过程。不仅涵盖了基础的首次部署，还重点介绍了如何利用 **Docker** 和 **VS Code Dev Containers** 搭建一套隔离、高效的本地写作环境。
## 1. 首次部署：让博客上线

### 创建仓库
1. 访问 GitHub 上 Chirpy 的官方 **[starter 模板仓库](https://github.com/cotes2020/chirpy-starter)**。
2. 点击右上角的绿色按钮 <kbd>Use this template</kbd>，选择 <kbd>Create a new repository</kbd>。
3. **关键步骤**：仓库名称必须严格遵守 `username.github.io` 的格式（其中 `username` 是你的 GitHub 用户名）。

### 设置自动化部署 (GitHub Actions)
由于 Chirpy 基于 Jekyll 开发，需要编译生成静态文件才能运行，因此不能使用 GitHub Pages 默认的部署方式，必须配置 **GitHub Actions**。
1. 进入创建好的仓库，点击上方的 <kbd>Settings</kbd> 选项卡。
2. 在左侧菜单栏找到 **Code and automation** 下的 <kbd>Pages</kbd>。
3. 将 **Build and deployment** 下面的 **Source** 选项从 `Deploy from a branch` 改为 <kbd>GitHub Actions</kbd>。

### 等待自动运行
1. 点击仓库上方的 <kbd>Actions</kbd> 标签页。
2. 你会看到一个名为 **Build and Deploy** 的任务正在运行（对应 Commit 信息通常为 **Initial commit**）。
3. 当图标变成✅绿色对勾时，表示渲染完成。此时访问 `https://username.github.io` 即可看到你的博客雏形。

---
## 2. 本地环境搭建：高效开发

虽然博客已经上线，但为了更方便地预览修改效果和撰写新文章，我们需要一个本地开发环境。

> **💡 为什么选择 Docker？**
> Windows 下配置 Ruby 环境极其繁琐且容易产生版本冲突。通过使用 **VS Code Dev Containers**，我们可以利用 Docker 快速构建一个包含所有依赖的隔离环境，**开箱即用，且不污染本地系统**。

### 环境准备
在开始之前，请确保你的电脑已安装以下工具：
*   **WSL 2** (Windows Subsystem for Linux)
*   **Docker Desktop** (请确保在设置中开启了 WSL 2 后端支持)
*   **VS Code** 及 **Dev Containers** 插件

### 容器构建
1. **Clone 仓库**：在 WSL (Ubuntu) 环境下克隆你的博客仓库，并使用 VS Code 打开该文件夹。
2. **启动容器**：打开后，VS Code 会检测到项目中的 `.devcontainer` 文件夹，右下角弹出提示框，点击 <kbd>Reopen in Container</kbd>。
3. **自动构建**：VS Code 会指挥 Docker 启动一个新的容器，并将当前仓库挂载进去。
4. **验证状态**：等待构建完成后，VS Code 左下角状态栏会显示：**Dev Container: Jekyll**，说明你已成功进入容器环境。

### 运行博客
此时你已经身处容器内部的终端（Terminal）中。按 ``Ctrl + ` `` 调出终端，运行以下命令启动服务：
```shell
bundle exec jekyll serve
```

打开浏览器访问 `http://localhost:4000`，即可实时预览本地环境下的网站。

---

## 3. 博客配置与创作

### 基本信息配置
网站的核心配置位于根目录的 `_config.yml` 文件中。建议优先修改以下几项：
*   `avatar`: 设置侧边栏头像（图片需放入 `/assets/img/`）
*   `timezone`: 修改为 `Asia/Shanghai`
*   `lang`: 修改为 `zh-CN` (简体中文)
*   `title` / `tagline`: 修改网站标题和标语

### 创建第一篇博客

#### 1. 创建文件
**⚠️ 警告：文件名格式至关重要！**
必须严格遵守 `YYYY-MM-DD-英文标题.md` 的格式，例如 `2024-01-22-hello-world.md`，否则 Jekyll 无法识别。

#### 2. 填写“文章头” (Front Matter)
文章顶部必须包含 YAML 格式的配置块：

```yaml
---
title: 我的第一篇博客文章  # 文章大标题
date: 2024-01-22 12:00:00 +0800  # 发表时间
categories: [生活, 记录]  # 分类：[大分类, 子分类]
tags: [心情, 测试]     # 标签：方便搜索
pin: true              # 是否置顶
math: true             # 是否启用数学公式渲染
mermaid: true          # 是否启用流程图渲染
---
```

#### 3. 撰写正文
正文支持标准的 Markdown 语法。
**引用图片：**
建议将图片存放在 `/assets/img/` 目录下：
`![描述](/assets/img/pic.jpg)`
**代码块：**
```python
print("Hello Chirpy")
```

---

### 🚀 效率提升：配置自动补全模板

每次手动输入 Front Matter 非常繁琐。我们可以利用 VS Code 的 **User Snippets (用户代码片段)** 功能实现“一键生成模板”。

#### 第一步：添加 Snippet
1. VS Code 菜单 -> `File` -> `Preferences` -> `Configure User Snippets`。
2. 选择 **Markdown** (`markdown.json`)。
3. 将以下配置粘贴到大括号 `{ ... }` 中：
```json
"Jekyll Post Header": {
    "prefix": "blog",
    "body": [
        "---",
        "title: ${1:标题}",
        "date: ${CURRENT_YEAR}-${CURRENT_MONTH}-${CURRENT_DATE} ${CURRENT_HOUR}:${CURRENT_MINUTE}:${CURRENT_SECOND} +0800",
        "categories: [${2:大类}, ${3:子类}]",
        "tags: [${4:标签}]",
        "pin: false",
        "math: false",
        "mermaid: false",
        "---",
        "",
        "## 简介",
        "",
        "${5:在这里写摘要}",
        "",
        "<!-- more -->",
        "",
        "## 正文",
        "",
        "$0"
    ],
    "description": "Insert Jekyll Front Matter with CS features"
}
```
#### 第二步：开启 Markdown 代码提示
VS Code 默认将 Markdown 视为纯文本，不主动触发代码提示。为了让上面的 `blog` 命令生效，我们需要修改 `settings.json`。

1. 按 `F1` (或 `Ctrl+Shift+P`)，输入 `Open User Settings (JSON)`。
2. 在文件中追加以下配置：
```json
"[markdown]": {
    "editor.quickSuggestions": {
        "other": true,
        "comments": true,
        "strings": true
    },
    "editor.snippetSuggestions": "top"
}
```

**✅ 效果：** 以后新建 `.md` 文件后，只需输入 `blog` 并回车，即可自动生成完整的文章结构。

---

## 4. 发布上线

本地预览确认无误后，最后一步就是将改动推送到 GitHub。

1. 打开 VS Code 左侧 **源代码管理 (Source Control)** 面板。
2. 输入 Commit Message，例如：`Add first post`。
3. 点击 **Commit** 提交。
4. 点击 **Sync Changes (Push)** 推送到远程仓库。

稍等几分钟，再次查看 GitHub Actions 状态，变绿后即可在你的个人域名下看到最新的文章了！
